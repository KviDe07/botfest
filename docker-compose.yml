services:
  bot:
    build: .
    # Авторестарт при сбое и после перезапуска Docker (если контейнер не останавливали вручную)
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/app/data
