FROM python:3.12-slim

WORKDIR /app

COPY backend/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY backend/ .

# Скрипты из корня репозитория (seed_events, migrate_json_to_pg)
COPY scripts/ ./scripts/

RUN mkdir -p data media

CMD ["python", "main.py"]
